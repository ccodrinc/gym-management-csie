/** Session duration in seconds (30 days) */
export const SESSION_MAX_AGE_SECONDS = 30 * 24 * 60 * 60

/** Default values for new gym class form */
export const DEFAULT_CLASS_DAY = 'Mon'
export const DEFAULT_CLASS_TIME = '08:00'
export const DEFAULT_CLASS_MAX_SPOTS = 20

/** Members can only keep up to this many active class bookings at once */
export const MAX_MEMBER_CLASS_ENROLLMENTS = 3
